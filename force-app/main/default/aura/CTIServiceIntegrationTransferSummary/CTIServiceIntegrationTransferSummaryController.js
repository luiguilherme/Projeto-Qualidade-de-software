({
    doInit : function(component, event, helper) {
        helper.createList(component, event, helper);
    },

    btnExitClick : function(component, event, helper) {
        helper.btnExitClick(component,event,helper);
    },
})